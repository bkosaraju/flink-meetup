<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Console appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%-5level- %logger{25}] - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Root logger level -->
    <root level="WARN">
        <appender-ref ref="STDOUT"/>
    </root>
    <Logger name="org.apache.flink" level="ERROR"/>
    <Logger name="org.apache.kafka" level="ERROR"/>
    <Logger name="org.apache.flinkmeetup" level="INFO"/>

    <!-- Optional: Silence all Flink logs -->
    <!--
    <logger name="org.apache.flink" level="ERROR" additivity="false">
        <appender-ref ref="STDOUT"/>
    </logger>
    -->

</configuration>
